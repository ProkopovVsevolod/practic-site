spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database: h2

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  test:
    database:
      replace: none

eureka:
  client:
    enabled: false